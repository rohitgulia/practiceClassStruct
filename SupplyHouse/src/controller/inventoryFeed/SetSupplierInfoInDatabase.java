package controller.inventoryFeed;

public class SetSupplierInfoInDatabase {

	public SetSupplierInfoInDatabase(SupplierProduct sp) {
		
		// go to supplier_product table
		// check if the entry is already there
		// if yes then update the table with new entry from supplierProduct or make new entry
		
	}
}
